<% var = params[:category] || 'total' %>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <div class="btn-group">
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
              <%= var.capitalize %> <span class="caret"></span>
            </button>
        <ul class="dropdown-menu" role="menu">
          <li><a href="revenue">Total</a></li>
          <% @revenues.column_names.each do |name| %>
            <% unless ['id', 'year', 'month', 'total'].include?(name) %>
              <li><a href="?category=<%= name %>"><%= name.capitalize %></a></li>
            <% end %>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12">
      <div id="monthly"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-6">
      <div id="total"></div>
    </div>
    <div class="col-sm-6">
      <div id="average"></div>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12"></div>
  </div>
</div>
</div>
<script>
$(function () {
  $('#monthly').highcharts({
    chart: {
      type: 'column'
    },
    title: {
      text: 'Monthly <%= var.capitalize %> Revenue'
    },
    xAxis: {
      categories: [
        <% @revenues.select(:month).distinct.each do |revenue| %>
          '<%= Date::MONTHNAMES[revenue.month] %>',
        <% end %>
      ]
    },
    yAxis: {
      title: {
        text: 'Revenue'
      },
      labels: {
        formatter: function() {
          return this.value;
        }
      }
    },
    tooltip: {
                headerFormat: '<span style="font-size:10px">{point.key}</span><table>',
                pointFormat: '<tr><td style="color:{series.color};padding:0">{series.name}: </td>' +
                    '<td style="padding:0"><b>{point.y:.1f}</b></td></tr>',
                footerFormat: '</table>',
                shared: true,
                useHTML: true
            },
    series: [
      <% @years.each do |year| %>
        {
          name: <%= year.year %>,
          data: [
            <% @revenues.year(year.year).each do |revenue| %>
              <%= revenue[var] %>,
            <% end %>
          ]
      },
      <% end %>
    ]
  });
  $('#total').highcharts({
    chart: {
      type: 'column'
    },
    title: {
      text: 'Annual <%= var.capitalize %> Revenue'
    },
    xAxis: {
      categories: [
        'Annual'
      ]
    },
    yAxis: {
      title: {
        text: 'Revenue'
      },
      labels: {
        formatter: function() {
          return this.value;
        }
      }
    },
    tooltip: {
                headerFormat: '<span style="font-size:10px">{point.key}</span><table>',
                pointFormat: '<tr><td style="color:{series.color};padding:0">{series.name}: </td>' +
                    '<td style="padding:0"><b>{point.y:.1f}</b></td></tr>',
                footerFormat: '</table>',
                shared: true,
                useHTML: true
            },
    series: [
      <% @years.each do |year| %>
        {
          name: <%= year.year %>,
          data: [
            <%= @revenues.year(year.year).sum(var) %>
          ]
      },
      <% end %>
    ]
  });
  $('#average').highcharts({
    chart: {
      type: 'column'
    },
    title: {
      text: 'Average <%= var.capitalize %> Revenue'
    },
    xAxis: {
      categories: [
        'Annual'
      ]
    },
    yAxis: {
      title: {
        text: 'Revenue'
      },
      labels: {
        formatter: function() {
          return this.value;
        }
      }
    },
    tooltip: {
                headerFormat: '<span style="font-size:10px">{point.key}</span><table>',
                pointFormat: '<tr><td style="color:{series.color};padding:0">{series.name}: </td>' +
                    '<td style="padding:0"><b>{point.y:.1f}</b></td></tr>',
                footerFormat: '</table>',
                shared: true,
                useHTML: true
            },
    series: [
      <% @years.each do |year| %>
        {
          name: <%= year.year %>,
          data: [
            <%= @revenues.year(year.year).average(var) %>
          ]
      },
      <% end %>
    ]
  });
});
</script>